<div>
  <h1 class="mb-6">Under construction...</h1>

  <%= form_with url: sims_path, method: :post, class: 'mb-3' do |form| %>
    <%= form.label :simc, "SimC Input", class: 'form-label' %>
    <%= form.text_area :simc, class: 'form-control', rows: 20 %>
    <%= form.submit "Run Sim", class: 'btn btn-primary' %>
  <% end %>
</div>
